# Core Data Display and Feedback principles review

## Scope and decision boundary

This is the family evidence matrix and Visual Language 1.0 application record for issue #138. It
reviews the published Tailwind-first Data
Display and Feedback inventory from `data/component-catalog.json`, including the shared Typography,
Icon, and Kbd foundations used by those families. The review covers semantic responsibility,
native or Base UI foundations, public API and anatomy, accessibility author responsibilities,
Tailwind and token contracts, package and source-install entrypoints, Registry metadata, docs,
CLI/MCP projections, fixtures, and contract/accessibility evidence.

The review preserves component responsibilities while applying the approved visual language. It
does not add a Core component, runtime axis, product workflow, or breaking API migration. Counter
and StatusDot are not present in the catalog at task start and are not added here. Compatibility
cleanup such as the deprecated Badge `variant` alias remains owned by the final API-freeze work in
#145.

## Family evidence matrix

| Component          | Responsibility and foundation                                                                                                                                                           | Public contract, states, and accessibility                                                                                                                                                                                                                             | Tailwind, distribution, projections, and evidence                                                                                                                                         | Result                                                                                                                                                     |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Typography         | Semantic Heading, Text, and inline Code primitives; native `h1`-`h6`, `p`, and `code`. Server entrypoint.                                                                               | Heading level is independent from visual size; Text has semantic tones; native props and refs remain available. Authors own document outline, labels, localization, long content, and reflow.                                                                          | Static Nerio-token recipes use `tailwindCn`; Registry `typography`, catalog, docs, package/source install, CLI/MCP, and contract coverage reviewed.                                       | **Fixed.** The documented `heading`, `text`, and `code` slots are now truthful and protected; docs maturity matches the canonical `stable-core` catalog.   |
| Icon               | Server-safe renderer for adapter-provided SVG components; the icons adapter owns the generic SVG contract and Lucide isolation.                                                         | Decorative by default; meaningful icons require a non-empty label. Icon-owned ARIA, non-focusable behavior, stroke forwarding, and custom SVG props remain protected.                                                                                                  | Static inline-size recipe; server export, foundation docs, adapter subpath, Registry dependency copies, package fixtures, and contract/a11y tests aligned.                                | **Aligned.**                                                                                                                                               |
| Kbd                | Supplemental native `kbd` notation, not an action or shortcut registrar. Server entrypoint.                                                                                             | Native props/ref and quiet static styling; authors add `aria-keyshortcuts` to the actual control and hide duplicated notation where appropriate. Contextual composition may replace its slot, as Button does with `button-kbd`.                                        | Registry `kbd`, catalog, docs, Button composition, source install, forced-colors and density evidence aligned.                                                                            | **Aligned.** Docs maturity now matches `stable-core`.                                                                                                      |
| Card and anatomy   | Generic surface or one-destination anchor; native section/article/div or anchor. KPI and product cards remain Pro or consumer composition.                                              | Default/secondary surfaces, inset/bleed visual, header/title/description/content/footer/action, semantic heading choice, link focus, narrow layout, refs. Linked Cards cannot contain nested actions.                                                                  | Static Card recipes and protected slots use `tailwindCn`; Registry, catalog, docs, CLI/MCP, package/source install, contract/a11y/browser coverage aligned.                               | **Aligned.**                                                                                                                                               |
| Badge              | Concise status, classification, or metadata; native span. Counting and overflow policy belong to Counter when that component exists.                                                    | `tone`, `emphasis`, `size`, decorative icons, loading, and deprecated migration-only `variant`/`icon` aliases. Text carries meaning; Badge is not interactive.                                                                                                         | Static status recipes, semantic/component tokens, Registry dependency on Spinner, docs, CLI/MCP, and tests reviewed.                                                                      | **Fixed.** Root slot, tone, size, emphasis, loading state, and `aria-busy` remain truthful after consumer props; the docs anatomy typo is corrected.       |
| Avatar             | Image and fallback identity presentation; native span, image, and fallback. Presence, identity policy, fetching, and account menus remain consumer or Pro concerns.                     | `name`, optional `src`/`alt`/fallback, decorative mode, sizes, image failure and source transitions. Meaningful output requires a non-empty normalized name or alt; blank alt falls back to the name.                                                                  | Static Avatar recipe, Registry, catalog, docs, package/source install, image transition tests, and a11y coverage reviewed.                                                                | **Fixed.** Accessible naming no longer falls through to an English runtime default or an empty image name; root slot and size hooks are protected.         |
| Stat               | One value with a label and optional textual trend; composed from Card. KPI systems, thresholds, comparisons, actions, and charts remain Pro.                                            | String label/value/trend, native Card root props/ref, static state, readable text without color-only direction.                                                                                                                                                        | Static Stat recipe plus Card dependency; Registry, catalog, docs, CLI/MCP and package/source install reviewed.                                                                            | **Fixed.** Registry/docs now describe the actual `card` root slot and current `--n-stat-*` token contract.                                                 |
| Table and anatomy  | Native tabular structure and resilient presentation. Fetching, sorting state, filtering, virtualization, editing, selection workflows, and persistence remain consumer or Pro concerns. | TableContainer plus caption/header/body/footer/row/head/cell; refs and native attributes; named focusable overflow opt-in; tbody-only hover/current/selected hooks; captions, scopes, headers, spans, sorting announcements, long content, RTL and keyboard scrolling. | Static logical-direction recipes and component tokens; Registry, catalog, docs, CLI/MCP, source-install, contract/a11y/browser evidence aligned.                                          | **Fixed.** Every public anatomy slot remains truthful after consumer attributes; the existing normalized TableContainer region contract remains unchanged. |
| List               | Short ordered or unordered structured records with optional native/router links. Feeds, selection, commands, routing policy, and entity schemas remain outside Core.                    | Stable item IDs, ordered markers, title/description/meta/leading/trailing anatomy, protected canonical href/link slot, forwarded link props, router element support, narrow wrapping and focus.                                                                        | Static List recipes, Registry, catalog, docs, CLI/MCP router/source-install fixtures, contract/a11y/browser evidence reviewed.                                                            | **Fixed.** The root slot is protected; existing item/link composition remains unchanged. Docs maturity now matches `stable-core`.                          |
| Item and ItemGroup | Generic content/media/description/actions anatomy and optional grouping without implicit interaction or list semantics. Entity rows and workflow selection remain consumer or Pro.      | `plain`/`outline`/`soft`, sizes, media variants, header/footer/actions/separator, semantic render element, composed refs, disabled/loading hooks, responsive wrapping. Authors choose native semantics and avoid nested actions in a clickable root.                   | Static logical recipes, Separator dependency, Registry, catalog, docs, CLI/MCP, source-install, ref/contract/a11y/browser evidence aligned.                                               | **Aligned.** Contextual ItemSeparator slot override remains intentional. Docs maturity now matches `stable-core`.                                          |
| Separator          | Native horizontal thematic break between related sections; spacing remains the default grouping tool.                                                                                   | Native `hr` props/ref, static state, no interaction or motion. ItemSeparator may intentionally make the rule decorative in grouped Item anatomy.                                                                                                                       | Static subtle-border recipe, Registry, catalog, docs, source install, and semantic coverage reviewed.                                                                                     | **Fixed.** Docs no longer advertise an unsupported vertical variant or describe the native root as decorative by default.                                  |
| KeyValue           | One compact definition-list label/value pair. Grouped record layouts remain consumer composition.                                                                                       | Native `dl`/`dt`/`dd`, React value content, root ref/props, static state, concise stable labels.                                                                                                                                                                       | Static semantic recipe, Registry, catalog, docs, package/source install and tests reviewed.                                                                                               | **Fixed.** The root anatomy hook is protected after consumer attributes.                                                                                   |
| Alert              | Persistent inline feedback; native div with opt-in live-region role. Dismissal and transient lifecycle belong to Toast.                                                                 | Semantic tones, optional decorative icon, title, description, action, native props/ref. Static alerts do not announce; authors opt into `status` or `alert` only for dynamic updates and keep meaning in text.                                                         | Static semantic status recipes, Registry, catalog, docs, CLI/MCP, source install and a11y tests reviewed.                                                                                 | **Fixed.** Root slot and tone hooks are protected without changing author-owned role semantics.                                                            |
| Toast              | Transient feedback lifecycle through Base UI; static Toast remains presentation-only. Notification centers, persistent history, and job trackers remain Pro or consumer workflows.      | Provider/manager/viewport, low/high priority, tones, action/loading/dismissal, localized labels, timeout pause/resume, F6 focus entry, logical/physical swipe, first-render and live RTL, safe areas, reduced motion.                                                  | Static Tailwind recipes plus allowlisted keyframes; client entrypoint, Button dependency, Registry, docs, CLI/MCP, clean installs, deterministic contract/a11y/browser evidence reviewed. | **Fixed.** Static root slot and tone remain truthful after DOM props. Managed lifecycle and the completed #190 RTL viewport correction remain aligned.     |
| Empty State        | Generic absence presentation with optional media, header, description, and actions. Recovery, onboarding, permission, and repair workflows remain consumer or Pro.                      | Sizes, start/center alignment, icon/illustration media, heading choice, horizontal/vertical actions, optional author-owned live role, mobile stacking and long content.                                                                                                | Static compound recipes, Registry, catalog, docs, CLI/MCP, package/source install, contract/a11y/browser evidence aligned.                                                                | **Aligned.**                                                                                                                                               |
| Progress           | Task-completion semantics, not a score or static measurement. Product orchestration and outcomes remain outside the primitive.                                                          | Exactly one naming path, normalized custom ranges, determinate/indeterminate/complete states, optional value text, protected progressbar anatomy, RTL, forced colors, and reduced motion.                                                                              | Static transform recipes and allowlisted indeterminate keyframe; Registry, catalog, docs, package/source install, CLI/MCP, contract/a11y evidence aligned.                                | **Aligned.**                                                                                                                                               |
| Skeleton           | Layout placeholder only; the surrounding region owns busy status and loading announcements.                                                                                             | Native div/ref/props, static loading state, always hidden from assistive technology, reduced-motion-safe placeholder.                                                                                                                                                  | Static muted-surface recipe plus shared motion class; Registry, catalog, docs, source-install closure and tests reviewed.                                                                 | **Fixed.** `aria-hidden` and the skeleton slot are protected; the public example no longer adds an inaccessible, ineffective label.                        |
| Spinner            | Short indeterminate loading indicator; native span. Long determinate tasks use Progress.                                                                                                | Standalone mode requires localized label and status semantics; decorative mode removes nested announcements; sizes, refs, currentColor, and reduced-motion static fallback.                                                                                            | Static recipe plus allowlisted `n-spin` keyframe; Registry, catalog, docs, package/source install, CLI/MCP and contract/a11y evidence aligned.                                            | **Aligned.**                                                                                                                                               |

## Cross-family conclusions

- Data Display remains presentation and semantic structure; product data policy, workflows, routing,
  persistence, entity schemas, and advanced dashboards remain consumer or Pro responsibility.
- Feedback remains local status and lifecycle communication; recovery flows, notification history,
  job orchestration, and business outcomes remain outside Core.
- Package and copied-source modes use the same complete static Tailwind recipes, Nerio variables,
  `tailwindCn`, Registry dependency graph, and server/client entrypoint split.
- The corrections keep existing anatomy, naming, documentation, and metadata contracts while
  refining approved component aliases and static Tailwind recipes. They add no prop, variant,
  runtime axis, component, or product behavior.
- No new focused follow-up issue is required. Breaking compatibility cleanup remains deferred to
  the existing #145 API-freeze work.

## Visual Language 1.0 application record

- Information hierarchy uses compact 12–14px supporting text, medium rather than semibold display
  weight, and larger size steps for headings and metric values.
- Card remains a generic raised white or black surface with no default border and a very soft shadow;
  the secondary surface remains flat and muted. Linked Cards, Lists, Items, and table rows use the
  shared hover, press, focus, and reduced-motion timing tokens.
- TableContainer groups data on a muted surface without an outer border. Hover and focus stay
  neutral; selected and current rows add a logical-edge indicator so state is not color-only.
- Avatar fallback fill and overlap border use fixed light/dark aliases, preventing translucent
  seams when avatars overlap while preserving all image, fallback, and decorative behavior.
- Alert is a flat muted inline surface. Status color is limited to its icon; title and description
  remain neutral and continue to carry the complete message in text.
- Toast is an inverted dark glass surface with blur, restrained semantic status icons, neutral
  white copy, and glass-aware action controls in both light and dark modes.
- Stat trends are neutral by default because Core cannot infer whether an arbitrary trend string is
  positive or negative. Progress remains the only default accent-colored feedback track; Spinner,
  Skeleton, and Empty State remain restrained and reduced-motion safe.

## Validation record

All applicable gates passed on the issue branch:

- `pnpm format:check`, `pnpm lint`, and `pnpm typecheck`;
- `pnpm test:catalog` (15 tests), `pnpm validate:catalog` (96 catalog entries, 42 installable
  identities, 96 matrix rows, 42 Registry items), `pnpm test:tokens` (47 tests),
  `pnpm validate:tokens` (818 definitions, 227 resolved references, 6 theme scopes, 42 Registry
  items), and `pnpm validate:runtime-axes` (6 themes, 3 modes, 2 densities);
- `pnpm validate:docs` (42 components and 818 tokens) and `pnpm test:docs-examples`;
- `pnpm test:ui` (146 tests) and `pnpm test:a11y` (18 tests);
- `pnpm test:cli`, `pnpm test:mcp`, `pnpm test:adapters`, and `pnpm pack:check` (all 6 public
  packages);
- `NERIO_RELEASE_EXPECT_PUBLIC=1 pnpm validate:release`, including packed package/source-install,
  CLI/MCP discovery, and clean Next.js consumer evidence;
- `pnpm build` (8 successful workspace build tasks, including both Next.js applications);
- `pnpm test:browser` (16 Chromium scenarios across docs and demo).
