# Nerio

Nerio is an open-source, source-first design system for modern digital products. It is universal and domain-agnostic: suitable for SaaS applications, consumer products, marketplaces, dashboards, internal tools, content platforms, creator tools, productivity products, and data-rich workflows.

Nerio has two product layers: Nerio Core and Nerio Pro.

Nerio Core is the open-source foundation: tokens, themes, primitive and base UI components, public docs, public registry, CLI, and public MCP/component discovery.

Nerio Pro is the paid advanced layer: complex product components, templates, premium themes, Figma assets, advanced registry items, and Pro MCP/AI tooling.

Core = building blocks. Pro = product-ready solutions.

Use `COMPONENTS.md` and `data/component-catalog.json` as the source of truth for Core/Pro component tiering.

Current roadmap focus is Phase 2A stabilization plus narrow task-approved Phase 2B Core coverage slices. Checkbox, Switch, RadioGroup, RadioGroupItem, FormGroup, and Sidebar Primitive are stable Core components; Breadcrumbs, Pagination, List, and Sheet are implemented Core components; Command Primitive and Pro components remain out of this slice. Sidebar Primitive owns layout, physical side, controlled or uncontrolled collapse state, regions, and accessible toggles; it does not own routes, navigation data, menus, permissions, persistence, or AppShell behavior. Compose Sheet explicitly for mobile and render one interactive navigation tree per viewport. Sheet is a Base UI modal side-panel primitive with explicit left/right/top/bottom sides, sm/md/lg sizes, compound slots, and no app navigation or settings behavior. RadioGroup supports concise data-driven `options` and compositional `RadioGroupItem` children; use composition for richer option content. A Core component should not move to `stable-core` until its API, accessibility, tokens, registry metadata, docs, CLI fixture, MCP metadata, and validation checks are aligned.

Use primitive tokens only for raw values. Use semantic tokens such as `--n-color-surface-default`, `--n-color-text-primary`, `--n-color-action-primary`, `--n-color-border-default`, `--n-font-sans`, and `--n-font-mono` in product code. Components should prefer component tokens for local contracts such as `--n-button-background-primary`, `--n-input-border`, `--n-dialog-width-md`, `--n-overlay-background`, and `--n-table-cell-padding-x`.

System UI is the default Core font stack and the cross-platform system mono stack is the default technical font. Geist is the documentation brand only. Core provides System, Geist, Inter, IBM Plex, Manrope, Source Sans 3, and Space Grotesk scoped token recipes; non-system families remain consumer-loaded, no font files are bundled, and components must consume font tokens rather than hard-coded families.

Nerio separates theme, mode, and density. Theme is selected with `data-theme="purple"`, `data-theme="blue"`, `data-theme="green"`, `data-theme="orange"`, `data-theme="red"`, `data-theme="neutral"`, or a custom product theme. Mode is selected with `data-mode="system"`, `data-mode="light"`, or `data-mode="dark"`. Density is selected with `data-density="comfortable"` or `data-density="compact"`.

Default runtime attributes: `data-theme="purple" data-mode="system" data-density="comfortable"`.

Do not create or use combined theme names such as `purple-light`, `purple-dark`, `neutral-light`, `neutral-dark`, `blue-light`, `blue-dark`, `red-light`, or `red-dark`. Do not create vertical-specific Core preset names such as `fintech-blue`; use generic brand color names.

Font, radius, motion, spacing, shadow/elevation, and contrast are token-customizable in v1, but they are not separate runtime axes. Do not introduce `data-font`, `data-radius`, `data-motion`, `data-contrast`, or `data-scale` unless a later architecture decision explicitly promotes them. Typography presets are CSS token recipes, not runtime attributes; customize typography with `--n-font-sans`, `--n-font-mono`, `--n-font-sans-system`, and scoped product overrides.

Brand color is a restrained accent. Use it for primary actions, selection, active states, focus, links, small progress signals, the primary chart series, and brand moments. Do not use brand color for broad page backgrounds, routine icons, card backgrounds, standard borders, or secondary actions.

Prefer composition, explicit slots, semantic HTML, visible focus states, and accessible labels. Use Nerio's icon adapter for Lucide or custom SVG React components.

Card anatomy is `Card`, optional `CardVisual`, `CardHeader`, `CardTitle`, `CardDescription`, optional `CardAction`, `CardContent`, and `CardFooter`. `CardVisual` accepts generic visual content and only supports `inset` or `bleed` placement. `CardAction` is for surface Cards only; linked Cards must not contain interactive descendants.

Package entrypoints: import static Core components and utilities from `@nerio/ui`, import interactive components from `@nerio/ui/client`, and import styles from `@nerio/ui/styles.css`. SidebarHeader, SidebarContent, SidebarFooter, and SidebarInset are server-safe; SidebarProvider, Sidebar, SidebarTrigger, SidebarRail, and useSidebar import from the client entrypoint. Example: `import { Alert, Breadcrumbs, Card, List, Pagination, SidebarContent, SidebarInset, Table } from "@nerio/ui"; import { Button, Select, Dialog, Sheet, Sidebar, SidebarProvider, SidebarTrigger, ToastProvider } from "@nerio/ui/client"; import "@nerio/ui/styles.css";`. Use Button's `link` variant with a native anchor render target for text-level navigation. Source-installed registry components keep local paths such as `@/components/nerio/components/button`.

Registry usage: `nerio init` creates `nerio.json`, `nerio list` lists component names, titles, and categories, `nerio info <component>` shows registry metadata, `nerio doctor` validates configuration and registry metadata, and `nerio add <component>` installs the selected source item plus registry dependencies. Use `nerio add input --dry-run` to preview files before writing. Registries may be local filesystem paths or HTTP URLs. Existing changed files are not overwritten unless `--overwrite` is explicitly provided.

`IconButton` remains installable as a deprecated compatibility alias until the next major release. New code must use `Button` icon-only mode: `<Button icon={Settings} aria-label="Workspace settings" />`.

The read-only MCP server uses the official Model Context Protocol stdio transport. Its tools are `list_components`, `get_component`, and `get_component_usage`; all responses come from the same Nerio registry metadata used by the CLI. `get_component_usage` returns usage, dependencies, registry dependencies, install files, Base UI primitives, slots, variants, states, required tokens, and accessibility guidance.

Registry `requiredTokens` should expose public customization points, preferably component aliases rather than internal implementation details. Registry `requiredTokens` and public component reference token chips must point to real `--n-*` CSS variables in `@nerio/tokens`; `pnpm validate:docs` checks this contract.

Progress is a server-safe Core component for completion of one task that takes time. It requires exactly one accessible name through a visible `label`, `aria-label`, or `aria-labelledby`; finite values are clamped within normalized `min` and `max` range values, while indeterminate Progress omits `aria-valuenow`. Use `valueText` for localized text such as "3 of 5 records imported". Progress is read-only, not a slider, and not a Meter for static scores, capacity, battery, or health values. Use Alert, Badge, or Toast for task outcomes.

Toast has separate static presentation and managed stack forms. Use `ToastProvider`, `ToastViewport`, and `useToastManager` for transient application feedback. Duplicate IDs update in place, `timeout={0}` creates an intentionally persistent toast, low priority is polite, and high priority is reserved for urgent failures. Managed timers preserve remaining duration during hover, keyboard focus, or window blur. The viewport supports safe areas, compact density, RTL-aware logical swipe, F6 keyboard entry, localized labels, and a dismiss button. Use one provider per stack; use `createToastManager` only for an intentionally independent provider. Toast is not a notification inbox, persistent history, or background-job manager.

Input is a server-safe thin native control for `text`, `email`, `password`, `search`, `tel`, `url`, and `number` values. Use `size="sm" | "md" | "lg"`, use `htmlSize` for the native HTML size attribute, and compose labels, descriptions, and messages through `Field`. Input does not own validation, visual prefix/suffix content, search results, password visibility, parsing, or date workflows. Use InputGroup for a shared Input surface with explicit `InputGroupAddon placement="start" | "end"`; addons do not provide the input's accessible name, and interactive addons remain independently accessible. SearchInput, PasswordInput, Calendar, and a basic single-date DatePicker are future Core candidates; date ranges, date-time and preset workflows, global/entity/advanced search, CommandPalette, and FilterBar belong to Pro.

AI agents should select the smallest component set that matches the workflow, avoid unnecessary domain-specific assumptions in Core, use Pro for advanced product workflows, keep examples universal unless the task explicitly asks for a vertical, and never introduce shadcn/ui, Radix UI, Headless UI, Ariakit, or overlapping primitive libraries.
