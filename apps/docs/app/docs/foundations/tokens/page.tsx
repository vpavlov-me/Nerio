import { StandardDocPage } from "../../../../components/doc-page";

export default function Page() {
  return (
    <StandardDocPage
      title="Tokens"
      lede="Nerio uses primitive, semantic, and component tokens so visual decisions remain portable across themes and products."
    />
  );
}
