import { StandardDocPage } from "../../../components/doc-page";

export default function Page() {
  return (
    <StandardDocPage
      title="Registry and CLI"
      lede="Nerio owns its registry format. The first vertical slice supports `nerio init`, `nerio add button`, and `nerio doctor`."
    />
  );
}
