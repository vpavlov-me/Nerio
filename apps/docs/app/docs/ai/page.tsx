import { StandardDocPage } from "../../../components/doc-page";

export default function Page() {
  return (
    <StandardDocPage
      title="AI tooling"
      lede="Nerio exposes component metadata, llms.txt, and read-only MCP tools so agents can discover and compose components safely."
    />
  );
}
