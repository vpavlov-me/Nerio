import { StandardDocPage } from "../../../../components/doc-page";

export default function Page() {
  return (
    <StandardDocPage
      title="Button"
      lede="Buttons trigger product actions with explicit intent, size, loading, icon, focus, density, and motion contracts."
      kind="button"
    />
  );
}
