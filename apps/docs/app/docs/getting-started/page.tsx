import { StandardDocPage } from "../../../components/doc-page";

export default function Page() {
  return (
    <StandardDocPage
      title="Getting started"
      lede="Install Nerio source components through the local registry or consume workspace packages while developing the foundation."
    />
  );
}
